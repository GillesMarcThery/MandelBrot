<Window x:Class="MandelBrot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MandelBrot"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600">
    <DockPanel>
        <Menu DockPanel.Dock="Top"  >
            <MenuItem Header="File">
                <MenuItem Header="_New File" />
                <MenuItem Header="_Open File"  />
                <MenuItem Name="MenuItem_Save_File" Header="_Save File" />
                <MenuItem Name="MenuItem_SaveAs_File" Header="_SaveAs File"  />
                <Separator />
                <MenuItem Header="Recent" x:Name="menuItem_recents_Files" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Parameter">
            </MenuItem>
            <MenuItem Header="_About">
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <Label Name="Label_PixelsXY" Content="xx"></Label>
            <Separator />
            <Label Name="Label_ReelXY" Content="xx"></Label>
            <Separator />
            <Label Name="Label_Divergence" Content="xx"></Label>
        </StatusBar>

        <Canvas DockPanel.Dock="Top" Name="myCanvas" Background="AntiqueWhite" SizeChanged="myCanvas_SizeChanged" MouseMove="myCanvas_MouseMove" MouseLeftButtonDown="myCanvas_MouseLeftButtonDown" MouseLeftButtonUp="myCanvas_MouseLeftButtonUp"/>
    </DockPanel>
</Window>
